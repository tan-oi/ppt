interface TableData {
    data : any;
    onChange : any
}

export const ChartDataTable : React.FC<TableData> = ({
    data,
    onChange
}) => {
        

    return (
        <>

        </>
    )
}